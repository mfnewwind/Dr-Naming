machine:
  #timezone: Asia/Tokyo
  node:
    version: 0.12
  post:
    - perl --version
    - java -version

dependencies:
  pre:
    - git submodule update --init

deployment:
  staging:
    branch: master
    heroku:
      appname: mfnewwind
