'use strict';
/*jslint node: true */

/**
 * 解析結果を取得する
 * @param repo   レポジトリ名 (例: github.com/user/repo)
 * @param branch ブランチ名 (例: master)
 * @param cb     コールバック関数 (引数: err, result)
 */
function load(repo, branch, cb) {
  
}

module.exports = load;